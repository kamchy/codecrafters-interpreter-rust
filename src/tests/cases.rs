
#[cfg(test)]
mod tests_main {
    // use crate::{evaluate, evaluate_with_code, utils};
    // use utils;

    // fn prepare() {
    //     let c  = utils::contents("cases.txt");
    //     for l in c.lines() {
    //         l.split("\t").take(2).co
    //     }
    // }

    // #[test]
    // fn test() {
    //     let (r, _) = evaluate_with_code(c.inp);
    //     match r {
    //         Ok(eres) => assert_eq!(
    //             c.outp,
    //             eres.to_string(),
    //             "Testing case {:?} got {}",
    //             c,
    //             eres
    //         ),
    //         Err(error) => panic!("case: {:?}, error: {}", c, error),
    //     }

    // }
}
